import {
  PrimaryButton,
  SecondaryButton,
  LinkButton,
} from './styles/button.styles';

const buttonMap = {
  primary: PrimaryButton,
  secondary: SecondaryButton,
  link: LinkButton,
};

export default buttonMap;
